{
  "name": "loom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-runtime agent harness for Codex CLI, Claude Code, Gemini CLI, and local agents.",
  "bin": {
    "loom": "bin/loom.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && tsc",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "doctor": "node bin/loom.js doctor"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.2.0",
    "ink-testing-library": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "ink": "^7.0.1",
    "react": "^19.2.0",
    "react-devtools-core": "^6.1.2"
  }
}
